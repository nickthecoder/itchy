${NAME} = Class({
    Extends: CostumePropertyScript,
    
    init: function() {
        // TODO Initialise each property to a default value.
    }
    
});
// TODO Define each property like so 
// CostumePropertyScript.addProperty("${NAME}", "exampleInteger", Integer, "Example Integer");

