${NAME} = Class({
    Extends: CostumePropertiesScript,
    
    init: function() {
        // TODO Initialise each property to a default value.
    }
    
});
// TODO Define each property like so 
// CostumePropertiesScript.addProperty("${NAME}", "exampleInteger", Integer, "Example Integer");

